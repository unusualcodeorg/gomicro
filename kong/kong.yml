_format_version: "2.1"
_transform: true

services:
  - name: auth
    url: http://auth:8080
    routes:
      - name: auth
        paths:
          - /auth

plugins:
  - name: apikey-auth-plugin
    config:
      auth_service_url: http://auth:8080